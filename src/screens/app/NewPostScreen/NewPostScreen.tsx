import React from 'react';

import { Screen, Text } from '@components';

export function NewPostScreen(){
    return(
        <Screen>
            <Text preset="paragraphLarge">New Post Screen</Text>
        </Screen>
    );
}
